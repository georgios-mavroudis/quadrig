## Important notes for the export
- There should be a process where the animations selected for export are baked into the deform bones into clean animations for export for the deform bones.
- For the export it should strip the rig from all the mch, org, ctl bones and keep just the deform bones. However we noticed that sometimes the bones with inherit rotation, and inherit scale they are not following the animation made by the control bones exactly
